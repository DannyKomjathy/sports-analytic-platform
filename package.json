{
  "name": "sports-analytics-platform",
  "version": "1.0.0",
  "description": "NBA Sports Analytics Platform with live odds and AI analysis",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:prod": "node server.production.js",
    "dev": "node server.js",
    "build": "cd react-dynamic && npm run build",
    "test": "echo \"No tests specified\" && exit 0",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "deploy": "npm run build && npm run docker:build && npm run docker:up"
  },
  "keywords": [
    "sports",
    "analytics",
    "nba",
    "odds",
    "react",
    "express"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}